#include "main.h"
#include <stdio.h>
/**
 * add - adds two integer and return thier results
 * @a: int to be added to b.
 * @b: int to be added to a.
 * Return: sum of a and b.
 */
int add(int a, int b)
{
	int n;

	n = a + b;

	return (n);
}
