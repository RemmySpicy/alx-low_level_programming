#include "main.h"

/**
 * mul - Multiplies two integer
 * @a: first value to be multiplied
 * #b: secons value to be multipied
 * 
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}
